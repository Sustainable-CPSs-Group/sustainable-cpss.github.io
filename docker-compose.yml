services:
  site:
    build:
      context: .
      target: development
    ports:
      - "4321:4321"
    volumes:
      - .:/site
      - node_modules:/site/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

volumes:
  node_modules:
